@page "/admin"
@model LogInModel
@{
    ViewData["Title"] = "Login";
}

<form asp-area="Admin" asp-action="/Auth/LogIn" method="post" role="form">
    <fieldset>
        <div class="alert alert-info">Username: admin, Password: admin</div>
        @if (ViewData["Fail"] != null)
        {
            <div class="alert alert-danger">Incorrect Username or Password!</div>
        }
        <div class="form-group">
            <input class="form-control" type="text" name="username" placeholder="Username" autofocus>
        </div>
        <div class="form-group">
            <input class="form-control" type="password" placeholder="Password" name="password" value="">
        </div>
        <input type="hidden" name="ReturnUrl" value="@Request.Query["ReturnUrl"]" />
        <!-- Change this to a button or input when using this as a form -->
        <button type="submit" class="btn btn-lg btn-success btn-block">Login</button>
    </fieldset>
</form>